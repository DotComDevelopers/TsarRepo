@model IEnumerable<TSAR.Models.Client>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutOther.cshtml";
}

<h2>Index</h2>
<p>
    @using (Html.BeginForm("Index", "Clients", FormMethod.Get))
    {
        <b>Search by:</b><font color="Black"> @Html.TextBox("Search")<input type="submit" value="Search" style="color: black" /></font>}
</p>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
      

        <th>
            <font color="white"> @Html.DisplayNameFor(model => model.ClientName)</font>
        </th>
        
        <th>
            <font color="white"> @Html.DisplayNameFor(model => model.ClientAddress)</font>
        </th>
        <th>
            <font color="white"> @Html.DisplayNameFor(model => model.ContactNumber)</font>
        </th>
        <th>
            <font color="white"> @Html.DisplayNameFor(model => model.Email)</font>
        </th>

        <th>
            <font color="white"> @Html.DisplayNameFor(model => model.ManageTravel.Rate)</font>
        </th>
        <th>
            <font color="white"> @Html.DisplayNameFor(model => model.ManageTravel.Distance)</font>
        </th>
        <th>
            <font color="white"> @Html.DisplayNameFor(model => model.ManageTravel.TravelCode)</font>
        </th>
        <th>
            <font color="white"> @Html.DisplayNameFor(model => model.ProjectLeader)</font>
        </th>



        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                <font color="gold">  @Html.DisplayFor(modelItem => item.ClientName)</font>
            </td>
            <td>
                <font color="gold">   @Html.DisplayFor(modelItem => item.ClientAddress)</font>
            </td>
            <td>
                <font color="gold">  @Html.DisplayFor(modelItem => item.ContactNumber)</font>
            </td>
            <td>
                <font color="gold">   @Html.DisplayFor(modelItem => item.Email)</font>
            </td>
            <td>
                <font color="gold" style="text-transform:uppercase;"> @Html.DisplayFor(modelItem => item.ManageTravel.Rate)</font>

            </td>
            <td>
                <font color="gold" style="text-transform:uppercase;"> @Html.DisplayFor(modelItem => item.ManageTravel.Distance)</font>

            </td>

            <td>
                <font color="gold" style="text-transform:uppercase;"> @Html.DisplayFor(modelItem => item.ManageTravel.TravelCode)</font>

            </td>

            <td>
                <font color="gold"> @Html.DisplayFor(modelItem => item.ProjectLeader)</font>
            </td>


            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
